name: deploy-dataadmin-oh

on:
  push:
    branches:
      - main
    paths:
      - 'dataadmin-oh/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: docker login -u $DOCKER_USERNAME -p $DOCKER_PASSWORD
        env:
          DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
          DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
      - run: cd dataadmin-oh && docker build -t ohbiohealth/dataadmin-oh .
      - run: docker push ohbiohealth/dataadmin-oh
      - uses: digitalocean/action-doctl@v2
        with:
          token: ${{ secrets.DIGITALOCEAN_ACCESS_TOKEN }}
      - run: doctl kubernetes cluster kubeconfig save ohbiohealth
      - run: kubectl rollout restart deployment dataadmin-oh-depl
